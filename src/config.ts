import type { ThemeConfig } from 'antd';
const config: ThemeConfig = {
  components: {
    Input: {
      // lineHeight: 3,
      // padding: 6,
      colorBorder: '#c1c1c1',
    },
  },
};


export default config;
